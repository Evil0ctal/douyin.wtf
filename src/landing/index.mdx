---
title: Douyin and TikTok data API, self-hosted
description: A self-hosted data API for Douyin and TikTok. REST, MCP and a web console, with an identity pool that maintains itself. Open source, free, and it runs on your own machine.
template: splash
hero:
  tagline: One <code>docker compose up</code> gives you a REST API, an MCP server and a web console for Douyin and TikTok — on your own machine, with an identity pool that maintains itself.
  image:
    file: ../../assets/logo.svg
  actions:
    - text: Try the live demo
      link: https://demo.douyin.wtf
      icon: rocket
    - text: Quick start
      link: /quickstart/
      icon: right-arrow
      variant: secondary
    - text: GitHub
      link: https://github.com/Evil0ctal/Douyin_TikTok_Download_API
      icon: external
      variant: minimal
head:
  - tag: script
    attrs:
      type: application/ld+json
    content: |
      {
        "@context": "https://schema.org",
        "@type": "SoftwareApplication",
        "name": "Douyin_TikTok_Download_API",
        "alternateName": "DTK",
        "applicationCategory": "DeveloperApplication",
        "operatingSystem": "Linux, macOS, Windows (Docker)",
        "url": "https://douyin.wtf",
        "codeRepository": "https://github.com/Evil0ctal/Douyin_TikTok_Download_API",
        "programmingLanguage": ["Python", "TypeScript", "Go"],
        "license": "https://www.apache.org/licenses/LICENSE-2.0",
        "isAccessibleForFree": true,
        "offers": { "@type": "Offer", "price": "0", "priceCurrency": "USD" },
        "description": "A self-hosted data API for Douyin and TikTok. REST, MCP and a web console, with a self-maintaining identity pool, a PostgreSQL archive and native request signing."
      }
---

import { Aside, Card, CardGrid, LinkCard, Steps, Tabs, TabItem } from '@astrojs/starlight/components'

<Aside type="tip" title="There is a live instance you can sign in to">
[**demo.douyin.wtf**](https://demo.douyin.wtf) runs this software and is open to
everyone. The login page fills in the demo account for you — no registration, no
key, nothing to install. [How to use it](#try-it-without-installing-anything).
</Aside>

## What it is

**Douyin_TikTok_Download_API** is an open-source, self-hosted data API for
[Douyin](https://www.douyin.com) (抖音) and [TikTok](https://www.tiktok.com). You run it
on your own machine with one `docker compose up`. There is no hosted tier, no
signup and no quota, and nothing is proxied through anybody else's server.

It fetches posts, authors, comments, mixes and search from both platforms, and it
downloads video and image albums **without a watermark** — it selects the clean
stream the platform already publishes rather than stripping anything. What it
collects is archived in your own PostgreSQL, so a post deleted upstream is still
readable locally.

<CardGrid stagger>
  <Card title="Three entry points, one service layer" icon="puzzle">
    A REST API with 93 operations, an MCP server for AI agents, and a `dtk` CLI.
    MCP is not a wrapper around HTTP: all three call the same service layer, so
    a tool call skips the second hop and the second authentication.
  </Card>
  <Card title="Cookies stop being a chore" icon="approve-check">
    A headless browser mints guest identities on a timer. The pool tests, cools
    and retires them on its own. Your own logged-in cookies are an optional
    upgrade, not a requirement.
  </Card>
  <Card title="Signing is native Python" icon="seti:python">
    `a_bogus`, `X-Bogus` and the TikTok signatures run in-process in
    microseconds. The browser stays as a fallback and as a drift detector — it
    is how the instance notices the platform changed its algorithm.
  </Card>
  <Card title="It tells you when it is unwell" icon="warning">
    Every request is logged with structure. Every identity and every endpoint
    carries a live health score. Risk-control signals trip a circuit breaker
    before the pool burns down.
  </Card>
</CardGrid>

## Try it without installing anything

<CardGrid>
  <Card title="In the browser" icon="laptop">
    <Steps>

    1. Open [demo.douyin.wtf](https://demo.douyin.wtf). The login page arrives
       with the demo account already filled in.
    2. Sign in. You land in the console as a `demo` role.
    3. Go to **Playground**, paste any Douyin or TikTok link, and press Parse.

    </Steps>
  </Card>
  <Card title="From a terminal" icon="seti:shell">
    Nothing to register for: the instance publishes its own demo password at
    `/api/v1/auth/demo`. The session is a cookie, not a bearer token.

    ```bash
    PASS=$(curl -s https://demo.douyin.wtf/api/v1/auth/demo | jq -r .data.password)

    curl -s -c jar -o /dev/null \
      -X POST https://demo.douyin.wtf/api/v1/auth/login \
      -H 'content-type: application/json' \
      -d "{\"username\":\"demo\",\"password\":\"$PASS\"}"

    curl -s -b jar -X POST 'https://demo.douyin.wtf/api/v1/parse?wait=25' \
      -H 'content-type: application/json' \
      -d '{"url":"https://www.douyin.com/video/7675505205047348543"}' \
    | jq '.data | {platform, kind, description}'
    ```
  </Card>
</CardGrid>

`?wait=25` is what makes the call synchronous: it holds the connection until the
task finishes and answers `200` with the result. Leave it off and you get `202`
with a task id instead, which is the normal asynchronous path. The ceiling on
this instance is 30 seconds.

The reference is open without a login too —
[`/swagger`](https://demo.douyin.wtf/swagger),
[`/redoc`](https://demo.douyin.wtf/redoc) and
[`/openapi.json`](https://demo.douyin.wtf/openapi.json), in both languages.

<Aside type="caution" title="What the demo is and is not">
It is one small server, shared by everyone, so it is rate-limited to **30
requests per 10 seconds**; going over puts you in a **10-second cooldown** where
every request is refused. That is the demo's limit, not the software's.

The `demo` role reads. It cannot manage identities, edit settings, create keys
or start downloads, and `explain` and `identity` in the Playground are
operator-only because both hand back a live credential. Anything you do there is
visible to whoever looks next — treat it as a public sandbox, and do not paste a
cookie into it.
</Aside>

## Common questions

<LinkCard title="Is this legal?" href="/faq/#is-this-legal" description="What the licence says, what it does not cover, and whose responsibility the answer is." />
<LinkCard title="Can it download without a watermark?" href="/faq/#can-it-download-without-a-watermark" description="Yes, when the platform serves one — and an exact account of what that means." />
<LinkCard title="Do I need cookies from my own account?" href="/faq/#do-i-need-cookies-from-my-own-account" description="No. Guest identities are minted for you; your own cookies widen what you can see." />
<LinkCard title="Do I need proxies?" href="/faq/#do-i-need-proxies" description="It depends entirely on your throughput. The honest version, with numbers." />
<LinkCard title="Does it support Bilibili, Xiaohongshu, Kuaishou or Weibo?" href="/faq/#does-it-support-bilibili-xiaohongshu-kuaishou-or-weibo" description="Not yet, and why the architecture leaves room for them." />

## Run your own

<Tabs>
  <TabItem label="Docker" icon="seti:docker">
    ```bash
    git clone https://github.com/Evil0ctal/Douyin_TikTok_Download_API.git
    cd Douyin_TikTok_Download_API
    cp .env.example .env          # then set DTK_SECRET_KEY
    COMPOSE_ENV_FILES=.env docker compose -p dtk -f docker/compose.yml up -d
    ```

    Open `http://localhost:8000` and finish the setup in the browser.
    The long version is in the [quick start](/quickstart/).
  </TabItem>
  <TabItem label="Guided installer" icon="seti:powershell">
    A bash script that works out your distribution, offers the right way to
    install Docker, scales the container limits to the machine, generates
    secrets and brings the stack up. Run it again afterwards and it becomes the
    operations menu: status, upgrade, passwords, backups, settings, disk.

    ```bash
    bash <(curl -fsSL https://raw.githubusercontent.com/Evil0ctal/Douyin_TikTok_Download_API/main/install/install.sh)
    ```

    There is a Chinese edition at `install/install.zh.sh`. Both are documented
    in [installation and deployment](/installation/).
  </TabItem>
</Tabs>

<CardGrid>
  <LinkCard title="Quick start" href="/quickstart/" description="The stack running on one host, an administrator account, and one link parsed — about ten minutes." />
  <LinkCard title="Installation and deployment" href="/installation/" description="The compose file service by service, every variable, reverse proxies, and running without Docker." />
  <LinkCard title="REST API guide" href="/api/" description="Authentication, the response envelope, the asynchronous task model, paging and errors." />
  <LinkCard title="MCP and AI agents" href="/mcp/" description="Point Claude Code, Claude Desktop or any MCP client at your own instance." />
</CardGrid>

---

Apache-2.0. Source, issues and releases on
[GitHub](https://github.com/Evil0ctal/Douyin_TikTok_Download_API) · try it at
[demo.douyin.wtf](https://demo.douyin.wtf) · read it in
[简体中文](/zh/).
